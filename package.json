{
  "name": "bezproblem",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && npm run prisma:generate --prefix backend",
    "start": "npm run prisma:migrate:deploy --prefix backend && npm run seed --prefix backend && node backend/src/index.js"
  }
}
